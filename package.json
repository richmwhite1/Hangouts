{
  "name": "hangout-app",
  "version": "1.0.0",
  "description": "Hangout 3.0 Application",
  "scripts": {
    "prebuild": "echo 'Installing dependencies...'",
    "build": "cd hangouts-3.0 && npm install && npx prisma generate && npm run build",
    "start": "cd hangouts-3.0 && npm start",
    "dev": "cd hangouts-3.0 && npm run dev",
    "deploy": "npm run build && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
